# Handoff package manifest — mirrors the research module's data-package manifest
# (docs/research/data-packages.md). Validated before dispatching paper-draft.
required_keys: [package, delivered, processing, artifacts]
artifact_keys: [id, file, kind, description]
kinds: [table, figure, stats, model-output, other]
